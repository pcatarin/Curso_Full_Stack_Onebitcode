<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1>Renderizando HTML dinâmico com EJS</h1>
    <p>
        <%= message %>
        <% if (1 === 2) { %>
         <p>Verdadeiro</p>
        <% } else { %>
         <p>Falso</p>   
        <% } %>
    </p>

    <ul>
        <% for( let index = 0; index <= 10; index++ ) { %>
            <li>4 X <%= index  %> = <%= (4*index)  %></li>
        <% } %>
    </ul>
</body>
</html>